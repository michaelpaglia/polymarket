# Polymarket Trading Bot - Python Dependencies
#
# Usage:
#   pip install -r requirements.txt
#
# Run sentiment bot only:
#   python -m src.main --paper          # Paper trading
#   python -m src.main --live           # Live trading
#
# Run both HFT + sentiment:
#   python scripts/run_all.py --paper   # Paper trading, both modules
#   python scripts/run_all.py --live    # Live trading, both modules
#   python scripts/run_all.py --hft-only --live   # HFT only
#   python scripts/run_all.py --sentiment-only    # Sentiment only

# Core
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
PyYAML>=6.0

# Console UI
rich>=13.0

# Logging
structlog>=24.0

# LLM (Gemini 3)
google-genai>=0.8

# HTTP client
httpx>=0.27

# Polymarket CLOB client
py-clob-client>=0.20

# Vector embeddings (for semantic market search)
sentence-transformers>=2.2
faiss-cpu>=1.7

# Web scraping (for news)
aiohttp>=3.9

# Utilities
numpy>=1.24
